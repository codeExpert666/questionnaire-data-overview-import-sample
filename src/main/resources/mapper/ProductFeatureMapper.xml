<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.acme.questionnaire.mapper.ProductFeatureMapper">
    <!--
        导入引用快照使用的产品-特性白名单。
        只有关系启用且特性字典仍启用时才允许写入评分或观点分类；停用特性即使历史关系仍为
        status=1，也不会进入新导入校验。
    -->
    <select id="selectEnabledProductFeatures" resultType="com.acme.questionnaire.ref.ProductFeatureRef">
        SELECT pf.product_id,
               pf.feature_id
        FROM pq_product_feature pf
        INNER JOIN pq_feature f ON f.id = pf.feature_id AND f.status = 1
        WHERE pf.status = 1
    </select>

    <!--
        配置页读取当前产品已勾选关系。
        同时要求特性字典仍启用，避免软删除特性继续以 selected=true 进入前端保存表单。
    -->
    <select id="selectEnabledFeatureIdsByProductId" resultType="long">
        SELECT pf.feature_id
        FROM pq_product_feature pf
        INNER JOIN pq_feature f ON f.id = pf.feature_id AND f.status = 1
        WHERE pf.product_id = #{productId}
          AND pf.status = 1
        ORDER BY pf.feature_id ASC
    </select>

    <!--
        整包保存前先软停用旧集合。
        这里不删除关系行，避免丢失历史配置痕迹；后续提交的 featureIds 会通过 upsert 重新置为启用。
    -->
    <update id="disableAllByProductId">
        UPDATE pq_product_feature
        SET status = 0
        WHERE product_id = #{productId}
          AND status = 1
    </update>

    <!--
        批量插入或恢复产品-特性关系。
        联合主键 (product_id, feature_id) 保证幂等；重复提交时刷新 status 和 updated_at，供导入
        引用快照立即读取到新的适用集合。
    -->
    <insert id="upsertProductFeatures">
        INSERT INTO pq_product_feature (
            product_id,
            feature_id,
            status
        ) VALUES
        <foreach collection="featureIds" item="featureId" separator=",">
            (
                #{productId},
                #{featureId},
                1
            )
        </foreach>
        ON DUPLICATE KEY UPDATE
            status = 1,
            updated_at = CURRENT_TIMESTAMP
    </insert>
</mapper>
